class Documento {
  constructor(ruta, extension, nombre, usuario) {
    this.ruta = ruta;
    this.extension = extension;
    this.nombre = nombre;
    this.usuario = usuario;
  }
}

module.exports = Documento;
